apiVersion: 1

datasources:
   - name: MySQL
     type: postgresql
     url: ${POSTGRESQL_HOST}:${POSTGRESQL_PORT}
     database: ${POSTGRESQL_DATABASE}
     user: ${POSTGRESQL_USER}
     secureJsonData: 
      password: ${POSTGRESQL_PASSWORD}
     access: proxy
     isDefault: true